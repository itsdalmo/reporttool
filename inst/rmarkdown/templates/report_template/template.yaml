name: reporttool beamer - dplyr (PDF)
description: >
  A template for creating beamer reports. Includes a few standard plots and keywords
  that can be replaced in batched reporting. Uses dplyr.
create_dir: true
